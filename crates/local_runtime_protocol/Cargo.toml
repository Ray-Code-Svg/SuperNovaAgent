[package]
name = "local_runtime_protocol"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
schemars = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

