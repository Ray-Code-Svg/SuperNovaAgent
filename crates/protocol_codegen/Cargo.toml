[package]
name = "protocol_codegen"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
local_runtime_protocol = { path = "../local_runtime_protocol" }
schemars = "0.8"
serde_json = "1"

